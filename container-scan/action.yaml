name: Container Security Scan
description: Scans container images for vulnerabilities using Wiz CLI

inputs:
  image-name:
    required: false
    description: Docker image name
  image-tag:
    required: true
    description: Docker image tag
  image-platform:
    description: Target platform to build image for (eg. linux/amd64 (default), linux/arm64, etc)
  enable-docker-build:
    required: false
    default: "true"
    description: Enable Docker build
  image-tags:
    required: false
    description: List of tags as key-value pair attributes
    default: ""
  artifactory-username:
    required: false
    description: Artifactory username
  artifactory-auth-token:
    required: false
    description: Artifactory auth token
  build-args:
    required: false
    description: List of build arguments for docker build as key-value pairs
  build-contexts:
    required: false
    description: List of additional build contexts (e.g., name=path)
    default: ""
  docker-config-file:
    required: false
    description: Path to the docker config file (defaults to .docker-config.json) Must contain imageName, may contain dockerfile
    default: .docker-config.json
  github-token:
    required: false
    description: GitHub token
  secrets:
    required: false
    description: List of secrets for docker build as key-value pairs (e.g., SECRET_KEY=VALUE)
  dockerhub-user:
    required: true
    description: DockerHub username
  dockerhub-password:
    required: true
    description: DockerHub password
  MAX_MIND_USER:
    required: false
    description: MaxMind User ID
  MAX_MIND_LICENSE_KEY:
    required: false
    description: MaxMind License Key
  vfunction-file:
    required: false
    description: Vfunction file name
  target:
    required: false
    description: Target build stage for multi-stage Docker builds
  wiz-client-id:
    required: true
    description: Wiz Client ID for service account authentication
  wiz-client-secret:
    required: true
    description: Wiz Client Secret for service account authentication

outputs:
  stdout:
    description: The complete output from the Wiz CLI scan
    value: ${{ steps.scan.outputs.stdout }}

runs:
  using: composite
  steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Download Wiz CLI
      run: |
        curl -o wizcli https://downloads.wiz.io/wizcli/latest/wizcli-linux-amd64
        chmod +x wizcli
        sudo mv wizcli /usr/local/bin/
      shell: bash

    - name: Authenticate with Wiz
      run: wizcli auth --id "$WIZ_CLIENT_ID" --secret "$WIZ_CLIENT_SECRET"
      env:
        WIZ_CLIENT_ID: ${{ inputs.wiz-client-id }}
        WIZ_CLIENT_SECRET: ${{ inputs.wiz-client-secret }}
      shell: bash

    - name: Build docker image
      if: ${{ inputs.enable-docker-build == 'true' }}
      run: |
        # Set up build context if provided
        BUILD_CONTEXT="."
        if [ -n "${{ inputs.build-contexts }}" ]; then
          BUILD_CONTEXT="${{ inputs.build-contexts }}"
        fi

        # Build the image with all provided arguments
        docker build \
          --platform ${{ inputs.image-platform || 'linux/amd64' }} \
          --tag ${{ inputs.image-tag }} \
          --build-arg VERSION=${{ github.sha }} \
          --build-arg REVISION=${{ github.sha }} \
          --build-arg BRANCH=${{ github.ref_name }} \
          --build-arg ARTIFACTORY_USERNAME=${{ inputs.artifactory-username }} \
          --build-arg ARTIFACTORY_AUTH_TOKEN=${{ inputs.artifactory-auth-token }} \
          --build-arg MAX_MIND_LICENSE_KEY=${{ inputs.MAX_MIND_LICENSE_KEY }} \
          ${{ inputs.build-args }} \
          ${{ inputs.target != '' && format('--target {0}', inputs.target) || '' }} \
          $BUILD_CONTEXT
      shell: bash

    - name: Scan Container Image
      id: scan
      run: |
        # Run the scan with enhanced options
        SCAN_OUTPUT=$(wizcli docker scan \
          --image ${{ inputs.image-tag }} \
          --policy "Default vulnerabilities policy" \
          --secrets \
          --sensitive-data \
          --show-vulnerability-details \
          --format human)

        # Save the output for later use
        echo "stdout<<EOF" >> $GITHUB_OUTPUT
        echo "$SCAN_OUTPUT" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
      shell: bash

    - name: Fetch digest of Docker image for Graph enrichment
      run: wizcli docker tag --image ${{ inputs.image-tag }}
      shell: bash

    - name: Cleanup Docker Image
      if: always()
      run: |
        docker image rm ${{ inputs.image-tag }} || true
      shell: bash
